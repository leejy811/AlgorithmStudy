#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0); 
    cin.tie(0);
    
    double A, B;
    
    cin >> A;
    cin >> B;
    
    cout.precision(12);
    cout << fixed;
    cout << A / B;
    
    return 0;
}